# 变更记录

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
镜像与版本一一对应：`ghcr.io/<owner>/mcp-toutiao:vX.Y.Z`。

## v0.1.0

### 新增

- 头条号扫码登录 + 双传输协议：扫码登录可在无头容器内完成，二维码以 MCP image 块返回给客户端扫码；同时提供 streamable-http（容器部署）与 stdio（npx 本地调用）两种传输方式。
- 发布图文文章与微头条：文章支持 Markdown 自动转头条富文本，可设置封面、正文插图、首发/合集/声明；微头条支持纯文本 + 最多 9 张配图 + 话题标签。

### 修复

这几个问题此前在假站测试里全部通过，只有对着真实头条号发布才暴露出来，是这一版“首个真机验证”的核心内容：

- 登录状态误判：未登录时头条的跳转链是 `/` → `/profile_v4/` → `/auth/page/login`，中间是一次客户端路由跳转，旧逻辑在跳转中途就误判“已登录”，导致后续操作在未登录状态下发起。改为等 URL 稳定不再变化后才判定登录完成。
- 发布按钮点击被拦截：编辑器右侧的 AI 助手抽屉没有遮罩层，却盖住了「预览并发布」的点击区域，点击落空却不报错。改为只隐藏这一个抽屉，避免连带关掉封面上传用的另一个抽屉。
- 谎报发布成功：此前拿不到任何发布证据时仍然返回 `success: true`，把失败当成功报给调用方。改为在证据不足时抛出 `PUBLISH_UNCONFIRMED`，并如实转述平台返回的 HTTP 200 及 `{code, message}` 原文。
- 多图连续插入替换 bug：逐张插图前会先点一下编辑器中央，而在 ProseMirror 里点中一张图会选中该节点，下一次粘贴就把它整个替换掉，最终只剩最后一张图。改为每次插图前把光标折叠到文末再粘贴。
- 「同时发布微头条」取消不掉：真站该行是两列结构，点左列标签毫无作用而 `click()` 却返回成功，
  于是一次调用会同时发出文章和微头条两条内容。改为回读 `input.checked` 并三级降级重试
  —— **点击成功不等于状态改变**。
- 图片外链发布报 `7115 图片uri非法`：贴外链图片时编辑器能正常显示，但发布时头条不认这个 uri。改为把图片当文件粘贴进编辑器，走平台自己的上传通道换取合法 uri。

### 变更

- 许可证由 Apache-2.0 改为 AGPL-3.0-only。
- Docker 基础镜像支持更换构建源，弱网环境下拉取镜像更稳定。

### 已知限制

- 暂不支持定时发布、频控与多账号并发（一个容器对应一个头条账号，操作按顺序串行执行）；镜像目前只提供 `linux/amd64` 架构；头条前端一旦改版可能导致页面选择器失效，选择器集中维护在 `src/core/selectors.ts`。
