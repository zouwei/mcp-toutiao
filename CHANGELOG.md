# 变更记录

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
镜像与版本一一对应：`ghcr.io/<owner>/mcp-toutiao:vX.Y.Z`。

## v0.1.0

首个发行版。**真实头条号验证通过**（此前所有测试都跑在本地假站上）。

### 能力

- 扫码登录（无头容器内可用，二维码以 MCP image 块返回）
- 发布图文文章：Markdown → 头条富文本，封面、正文插图、首发/合集/声明
- 发布微头条：纯文本 + 最多 9 张配图 + 话题
- 双传输：streamable-http（容器）与 stdio（npx）

### 真机验证中修掉的问题

这些都是**假站测试全绿、真站才暴露**的，记在这里是为了说明"验证通过"具体指什么：

- **登录状态误判**：未登录时头条的跳转链是 `/` → `/profile_v4/` → `/auth/page/login`，
  中间那跳是客户端跳转。旧实现在中途就判定"已登录"。改为等 URL 不再变化。
- **发布点击被拦**：编辑器右侧的 AI 助手抽屉没有遮罩层，却盖住「预览并发布」拦截点击。
  改为定向隐藏该抽屉（不能连坐关掉所有 drawer —— 封面上传用的是另一个）。
- **谎报发布成功**：拿不到任何发布证据时旧实现仍返回 `success: true`。
  改为抛 `PUBLISH_UNCONFIRMED`，并把平台的原话（HTTP 200 + `{code, message}`）如实转述。
- **`7115 图片uri非法`**：贴外链图时编辑器只是"显示"，发布时平台不认那个 uri。
  改为**把图片当文件粘贴进编辑器**，走平台自己的上传通道。
- **多图只剩最后一张**：逐张插图前点了编辑器中央，而点中图片在 ProseMirror 里等于选中该节点，
  下一次粘贴把它替换掉。改为把光标折叠到文末再粘贴。
- **「同时发布微头条」取消不掉**：真站该行是两列结构，点左列标签毫无作用而 `click()` 却成功。
  改为回读 `input.checked` 并三级降级重试。

### 已知限制

- 未提供定时发布、频控与多账号并发（一个容器一个账号，操作串行）
- 镜像只出 `linux/amd64`
- 头条前端改版会导致选择器失效；选择器集中在 `src/core/selectors.ts`
